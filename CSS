/* --- Basic Reset & Global Styles --- */
:root {
    /* --- Updated Engineering Color Palette --- */
    --primary-color: #60a5fa; /* Engineering blue */
    --secondary-color: #0f172a; /* Dark technical background */
    --surface-color: #111827; /* Panel surface */
    --accent-color: #f59e0b; /* Amber highlight */
    --text-color: #e2e8f0;
    --light-text: #f8fafc;
    --border-color: rgba(148,163,184,0.16);
    --font-heading: 'Poppins', sans-serif;
    --font-body: 'Lato', sans-serif;
}

body {
    margin: 0;
    font-family: var(--font-body);
    color: var(--text-color);
    line-height: 1.6;
    background: var(--secondary-color);
}

.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
}

h1, h2, h3 {
    font-family: var(--font-heading);
    color: var(--primary-color);
}

/* --- Header & Navigation --- */
.main-header {
    background: rgba(15, 23, 42, 0.95);
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 20px 60px rgba(0,0,0,0.25);
}
.main-header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.logo {
    text-decoration: none;
    display: inline-block; /* Helps contain the image */
}
.logo img {
    height: 50px; /* Set a fixed height for the logo */
    width: 50px; /* Set equal width for a perfect circle */
    border-radius: 50%; /* --- THIS MAKES THE LOGO ROUND --- */
    object-fit: cover; /* Ensures the image covers the circle without distortion */
    display: block; /* Removes any extra space below the image */
}
.main-nav ul {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
}
.main-nav a {
    color: var(--primary-color);
    text-decoration: none;
    padding: 0.5rem 1rem;
    font-weight: 700;
    transition: color 0.3s ease;
}
.main-nav a:hover {
    color: var(--accent-color);
}

/* --- Hero Section --- */
.hero {
    /* --- UPDATED: Engineering-inspired background --- */
    background: radial-gradient(circle at top left, rgba(96,165,250,0.18), transparent 25%),
                linear-gradient(180deg, rgba(15,23,42,0.98), rgba(17,24,39,0.95));
    color: var(--light-text);
    text-align: center;
    padding: 120px 0; /* Generous padding for a full-screen feel */
    border-bottom: 1px solid var(--border-color);
}

.hero h1 {
    color: var(--light-text);
    font-size: 3rem;
    margin-bottom: 1rem;
}
.hero p {
    font-size: 1.2rem;
    max-width: 600px;
    margin: 0 auto 2rem auto;
}
.btn {
    display: inline-block;
    background: var(--accent-color);
    color: var(--primary-color);
    padding: 12px 30px;
    text-decoration: none;
    font-weight: 700;
    border-radius: 5px;
    transition: background 0.3s ease, transform 0.3s ease;
}
.btn:hover {
    background: rgba(245,158,11,0.9);
    color: var(--secondary-color);
    transform: translateY(-2px);
}
.btn-secondary {
    background: rgba(248,250,252,0.08);
    color: var(--light-text);
    border: 2px solid rgba(248,250,252,0.25);
    margin-left: 10px;
}
.btn-secondary:hover {
    background: rgba(248,250,252,0.16);
    color: var(--primary-color);
}

/* --- Services Section --- */
.services-section {
    padding: 60px 0;
    background: rgba(96,165,250,0.06);
}
.services-section .section-title {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 40px;
}
.services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
}
.service-card {
    background: var(--surface-color);
    padding: 30px;
    text-align: center;
    border-radius: 12px;
    border: 1px solid rgba(96,165,250,0.12);
    box-shadow: 0 16px 50px rgba(15,23,42,0.22);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.service-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
}
.service-card .icon {
    font-size: 3rem;
    color: var(--accent-color);
    margin-bottom: 15px;
}
.service-card h3 {
    margin-bottom: 10px;
}

/* --- CTA Section --- */
.cta-section {
    background: var(--primary-color);
    color: var(--light-text);
    text-align: center;
    padding: 60px 0;
}
.cta-section h2 {
    color: var(--light-text);
    font-size: 2.5rem;
}
.cta-section .btn {
    margin-top: 20px;
}

/* --- Footer --- */
.main-footer {
    background: rgba(15, 23, 42, 0.95);
    color: #cbd5e1;
    text-align: center;
    padding: 30px 0;
    border-top: 1px solid rgba(96,165,250,0.12);
}
.main-footer p {
    margin: 0;
}

/* --- Responsive Design --- */
@media (max-width: 768px) {
    .main-header .container {
        flex-direction: row; /* Keep logo and toggle button on the same line */
        align-items: center;
    }
    .hero h1 {
        font-size: 2rem;
    }
    .btn-secondary {
        display: block;
        margin-left: 0;
        margin-top: 10px;
    }
}


/* --- ADDED: Mobile Navigation Styles --- */
.nav-toggle {
    display: none; /* Hidden on desktop */
    background: transparent;
    border: 0;
    color: var(--primary-color);
    font-size: 1.5rem;
    cursor: pointer;
    padding: 0.5em;
}

.hamburger {
    display: block;
    position: relative;
}

.hamburger,
.hamburger::before,
.hamburger::after {
    background: var(--primary-color);
    width: 2em;
    height: 3px;
    border-radius: 1em;
    transition: transform 250ms ease-in-out;
}

.hamburger::before,
.hamburger::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
}

.hamburger::before { top: 6px; }
.hamburger::after { bottom: 6px; }

/* Styles for when the nav is open on mobile */
.nav-open .hamburger {
    transform: rotate(.625turn);
}
.nav-open .hamburger::before {
    transform: rotate(90deg) translateX(-6px);
}
.nav-open .hamburger::after {
    transform: rotate(90deg) translateX(6px);
}

/* Media query to show the hamburger menu on smaller screens */
@media (max-width: 768px) {
    .nav-toggle {
        display: block;
    }

    .main-nav {
        position: fixed;
        top: 0;
        left: 0;
        bottom: 0;
        right: 0;
        background: var(--light-text);
        transform: translateX(100%);
        transition: transform 250ms cubic-bezier(.5, 0, .5, 1);
        z-index: 999; /* Below header (1000) but above content */
    }

    .main-nav ul {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        flex-direction: column;
        align-items: center;
    }
    
    .main-nav a {
        font-size: 1.5rem;
        padding: 1rem;
    }

    /* This class is added by JavaScript to show the menu */
    .nav-open .main-nav {
        transform: translateX(0);
    }
}
